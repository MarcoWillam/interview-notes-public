# API base URLs include the version prefix, for example https://your-provider.example/v1.
# Leave blank until a provider is selected. Never expose API keys through NEXT_PUBLIC_*.
ASR_BASE_URL=
ASR_API_KEY=
ASR_MODEL=
ANALYSIS_BASE_URL=
ANALYSIS_API_KEY=
ANALYSIS_MODEL=

# Legacy audio adapters only; the Markdown workflow does not use ASR.
ASR_PROVIDER=
ASR_RESOURCE_ID=
