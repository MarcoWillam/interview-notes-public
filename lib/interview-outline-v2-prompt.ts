export const resumeOutlineV2Instructions =
  'outline 必须生成结构化 V2 面试提纲：version 固定为 2；requiredQuestions 恰好五道必问题，reserveQuestions 最多三道候选题，archivedReserveQuestions 初次生成返回空数组。主问题必须是面试官可以直接念出的 8–24 个字符短句，只验证一个核心判断，最多一个问号；背景、过程、行动、结果和复盘等细节放入 goal、listenFor、riskSignals 与条件 probes。每题预计 3–7 分钟，五道必问题预计用时之和不能超过 32 分钟，estimatedMinutes 必须等于该总和。每题只能有一个主评估维度，最多两个辅助评估维度，名称必须逐字取自 dimensionText。必问题必须以主维度覆盖自驱力与结果闭环以及 dimensionText 的前四项岗位专业能力；全部必问题和候选题合计必须覆盖输入的八个维度。coverage 必须根据当前必问题与候选题逐项精确计算：主维度写入 primaryQuestionIds，辅助维度写入 secondaryQuestionIds；有主覆盖为 covered，只有辅助覆盖为 weak，无覆盖为 uncovered，最终不得有 uncovered。每题 id 唯一，当前问题文本不重复。source=resume 时 resumeEvidence 必须是 resumeText 中逐字连续原文且 workSampleEvidence=null；source=role 或 written-test 时两类 evidence 都为 null；source=work-sample 时 resumeEvidence=null 且必须返回安全的相对文件路径和逐字引用。AI 产品经理在提供作品时，第 2–4 道必问题必须且只能是 work-sample 来源；没有作品但 hasWrittenTest=true 时，第 2–4 道必问题必须且只能是 written-test 来源，围绕既有笔试的统一考察目的核实候选人自己的判断，不得假装知道答卷内容；无作品且 hasWrittenTest=false 时不得使用 written-test 或 work-sample 来源。产品运营不得使用 written-test 或 work-sample 来源，即使输入状态包含笔试。每道题都要写清验证目标、1–3 个观察点、1–3 个风险信号和 1–2 个条件追问。只返回符合给定结构的 JSON。';
